# Pipeline migrada para Oracle VM (cron 0 12 * * 1,3,5 via run-lead-finder.sh)
# GitHub Actions não funciona com Outscraper — IPs de datacenter são bloqueados.
# Este workflow está desativado. Execução real: Oracle VM 147.15.23.161

name: GBP Lead Finder (desativado)

on:
  workflow_dispatch:

jobs:
  find-leads:
    runs-on: ubuntu-latest
    steps:
      - name: Pipeline migrada para Oracle VM
        run: echo "Lead finder roda no Oracle VM via cron (Seg/Qua/Sex 09h Brasilia). Outscraper bloqueia IPs de datacenter."
